{
  "name": "aram-builds-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker for LoL Build Aggregator",
  "main": "worker.js",
  "scripts": {
    "build": "node utils/convert-json-to-js.js",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
